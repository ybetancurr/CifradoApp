<UserControl x:Class="CifradoApp.Forms.DecryptForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F4F4F6">

    <UserControl.Resources>

        <Style x:Key="ActionButton" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                Padding="0">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryButton" TargetType="Button" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="#EDEDF3"/>
            <Setter Property="Foreground" Value="#555566"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderBrush="#DDDDE8"
                                BorderThickness="1">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E0E0EC"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,28,28,28" MaxWidth="400"
                    HorizontalAlignment="Center">

            <!-- Encabezado -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                <TextBlock Text="🔓" FontSize="22" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBlock Text="Descifrar número"
                           FontSize="20"
                           FontWeight="SemiBold"
                           Foreground="#1A1A2E"
                           VerticalAlignment="Center"/>
            </StackPanel>
            <TextBlock Text="Ingresa el número cifrado para recuperar el valor original."
                       FontSize="12"
                       Foreground="#888899"
                       Margin="0,0,0,28"
                       TextWrapping="Wrap"/>

            <!-- Card principal -->
            <Border Background="#FFFFFF"
                    BorderBrush="#E5E5EA"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="22,20">
                <StackPanel>

                    <!-- Label input -->
                    <TextBlock Text="Número cifrado"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="#555566"
                               Margin="0,0,0,6"/>

                    <!-- TextBox estilizado -->
                    <Border BorderBrush="#DDDDE8"
                            BorderThickness="1"
                            CornerRadius="8"
                            Margin="0,0,0,20">
                        <TextBox x:Name="txtNumero"
                                 Height="40"
                                 FontSize="18"
                                 FontWeight="SemiBold"
                                 MaxLength="6"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Foreground="#1A1A2E"
                                 Padding="12,0"
                                 VerticalContentAlignment="Center"
                                 CaretBrush="#1D9E75">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                    </Border>

                    <!-- Botones -->
                    <Grid Margin="0,0,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="12"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <Button x:Name="btnDescifrar"
                                Grid.Column="0"
                                Content="Descifrar"
                                Style="{StaticResource ActionButton}"
                                Background="#1A1A2E"
                                Foreground="#FFFFFF"
                                Click="btnDescifrar_Click"/>

                        <Button x:Name="btnLimpiar"
                                Grid.Column="2"
                                Content="Limpiar"
                                Style="{StaticResource SecondaryButton}"
                                Click="btnLimpiar_Click"/>
                    </Grid>

                </StackPanel>
            </Border>

            <!-- Card resultado -->
            <Border x:Name="resultCard"
                    Background="#FFFFFF"
                    BorderBrush="#E5E5EA"
                    BorderThickness="1"
                    CornerRadius="12"
                    Padding="22,18"
                    Margin="0,12,0,0"
                    Visibility="Hidden">
                <StackPanel>
                    <TextBlock Text="RESULTADO"
                               FontSize="10"
                               FontWeight="SemiBold"
                               Foreground="#AAAABC"
                               
                               Margin="0,0,0,8"/>
                    <TextBlock x:Name="lblResultado"
                               FontSize="28"
                               FontWeight="SemiBold"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Foreground="#1A1A2E"/>
                    <TextBlock x:Name="lblSubtitulo"
                               FontSize="12"
                               TextAlignment="Center"
                               Foreground="#888899"
                               Margin="0,4,0,0"/>
                </StackPanel>
            </Border>

        </StackPanel>
    </ScrollViewer>
</UserControl>